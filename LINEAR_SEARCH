LINEAR_SEARCH

class Solution {
    public int binarysearch(int[] arr, int k) {
        int n=arr.length;
        int index=-1;
        for(int i=0;i<n;i++){
            if(arr[i]==k){
                index = i;
          System.out.print(index);
            }
        }
